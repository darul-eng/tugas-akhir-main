extend type Mutation{
    login(email: String!, password: String!): String! @field(resolver: "Authentication@login")
    logout: User @field(resolver: "Authentication@logout")
}

extend type Query{
    me: User! @auth @guard
}